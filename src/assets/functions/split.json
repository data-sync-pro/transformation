{
  "title": "SPLIT",
  "description": "Returns an array of substrings by splitting a string using a regular expression delimiter.",
  "syntax": "SPLIT(string, regex) -> String[]",
  "parameters": [
    {
      "name": "string",
      "type": "String",
      "required": "Yes",
      "description": "The input string to split."
    },
    {
      "name": "regex",
      "type": "String",
      "required": "Yes",
      "description": "The regular expression used as the delimiter."
    }
  ],
    "examples": [
      "SPLIT(\"Sales, Marketing;  Support,  Finance\", \"[,;\\s]+\")\n-- Returns [\"Sales\", \"Marketing\", \"Support\", \"Finance\"]."],
    "tips": [
  ],
  "relatedFormula": []
}
