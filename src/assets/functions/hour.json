{
  "title": "HOUR",
  "description": "Extracts the hour (0–23) from a Datetime value, based on the context user’s local time zone.",
  "syntax": "HOUR(datetime) -> Integer",
  "parameters": [
    {
      "name": "datetime",
      "type": "Datetime",
      "required": "Yes",
      "description": "The Datetime value from which the hour is extracted"
    }
  ],
  "examples": [
    "HOUR(NOW())\n-- Returns 8 (if now = 2024-09-18T15:25:22.418Z and user is in PDT)."
  ],
  "tips": [],
  "relatedFormulas": ["NOW"]
}