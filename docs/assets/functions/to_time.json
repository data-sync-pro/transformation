{
  "title": "TO_TIME",
  "description": "<p>Converts a time string from the context user's local time zone into a standardized UTC Time.</p>",
  "syntax": "TO_TIME(string) -> Time",
  "parameters": [
    {
      "name": "string",
      "type": "String",
      "required": "Yes",
      "description": "The string to convert into a Time, provided in a recognized time format (e.g., 'HH:mm:ss')."
    }
  ],
  "examples": [
    "TO_STRING(TO_TIME(\"14:30:00\"))\n-- Returns \"19:30:00.000Z\" if the time is converted and displayed in UTC.\n-- The output reflects the time adjusted from local to UTC, depending on the context user's time zone."
  ],
  "tips": [
    {
      "text": "<p>Accepted formats include: HH:mm:ss, HH:mm:ss.SSS, HH:mm:ss.SSSZ, HH:mm, and H.</p>",
      "children": []
    }
  ],
  "relatedFormulas": [
    "TO_STRING"
  ]
}